<h1>Listing openid_users</h1>

<table>
  <tr>
  </tr>

<% @openid_users.each do |openid_user| %>
  <tr>
    <td><%= link_to 'Show', openid_user %></td>
    <td><%= link_to 'Edit', edit_openid_user_path(openid_user) %></td>
    <td><%= link_to 'Destroy', openid_user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New openid_user', new_openid_user_path %>